export const host = "http://localhost:8000";

// userRoutes
export const registerRoute = `${host}/api/user/register`;

export const loginRoute = `${host}/api/user/login`;

export const uploadPhoto = `${host}/api/upload`;

export const createProduct = `${host}/api/product/addProduct`;

export const getAllProducts = `${host}/api/product/getAllProducts`;
